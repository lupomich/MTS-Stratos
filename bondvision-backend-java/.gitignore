.gradle/
build/
.idea/
*.iws
*.iml
*.ipr
out/

# Never commit certificates or secrets
certs/
libs/*.jar

# Keep the classes.csv but not the market JAR
!libs/.gitkeep

# Build output — generated by: gradle shadowJar --no-daemon
# The fat JAR must be present in build/libs/ before running docker build.
# DO NOT commit the JAR — rebuild as needed.
# build/libs/*.jar  ← intentionally NOT ignored here so Docker COPY can find it
